# AllWorld Factures — Phone-friendly prototype

This version is intentionally packaged as a static single-page app so it can be uploaded to GitHub from a phone without Termux.

Files:
- `index.html` — full responsive app prototype
- `vercel.json` — Vercel SPA routing

Import the repository in Vercel with Framework Preset: **Other**.
Deployment trigger
